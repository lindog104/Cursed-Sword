extends State

## Slime Attack Cooldown State
#
# Handles Slime's attack cooldown behavior
# Transitions to Attack or to Follow depending on target distance

# Called when transitioning into this state 
func enter()-> void:
	pass

# Called when transitioning out of this state
func exit() -> void:
	pass

# Called every frame. 'delta' is the time between frames
func update(_delta: float) -> void:
	pass

# Called every physics frame. 'delta' is the time between frames
func physicsUpdate(_delta: float) -> void:
	pass
